# Game icons

Put PNG files here using the paths listed in `backend/data/raw_data.json`.

For example, `resources/food.png` is available in the app as `/images/resources/food.png`.

Shared icon frames belong in `icon-frames/`, for example `icon-frames/resource_base.png`.
